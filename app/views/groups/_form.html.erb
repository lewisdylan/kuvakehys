<%= form_for(@group) do |f| %>
  <% if @group.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@group.errors.count, "error") %> prohibited this group from being saved:</h2>

      <ul>
      <% @group.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div>
   Alle Fotos, die an:
    <%= f.text_field :email %>@kuvakehys.de 
    geschickt werden, sollen sobald
    <%= f.text_field :photo_limit %> Fotos gesammelt wurden, 
    entwickelt und per Post an
    <%= f.text_area :address %>
    versendet werden. 

  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
