<div id="group-details">
  E-mail photos to this address:
  <h1 class="group-email animated">
    <%= link_to @group.email_address, "mailto:#{@group.email_address}" %>
  </h1>
  <p>
    Once the album has collected <%= @group.photo_limit %> the photos will be printed and shipped.
  </p>
  <p>
    <strong>Only <%= @group.photos_missing_for_next_order %> photos are missing for the next album!</strong>
  </p>


  <div class="call-to-action small">
  </div>

  <% if @manage %>
    <hr />
    <div class="pure-g">
      <div class="pure-u-1-3">

        <h4>Recipients (<%= link_to "+", new_group_recipient_url(@group) %>)</h4>
        <% @group.recipients.each do |recipient| %>
          <%= recipient.name %> <% if @group.recipients.many? %> (<%= link_to "-", group_recipient_url(@group, recipient), method: :delete %>)<% end %><br />
          <%= recipient.address_1 %><br />
          <% if recipient.address_2? %>
            <%= recipient.address_2 %><br />
          <% end %>
          <%= recipient.postal_code %> <%= recipient.city %> - <%= recipient.country %>
          <hr />
        <% end %>
      </div>

      <div class="pure-u-1-3">
        <h4>Billing information</h4>
        <p>oh, lucky you! we haven't built the billing part yet, that means all prints are FREE for you</p>
      </div>

      <div class="pure-u-1-3">
        <h4>Statistics</h4>
        Photos: <%= @group.photos.count %><br />
        Albums: <%= @group.orders.count %><br />
        currently missing: <%= @group.photos_missing_for_next_order %><br />

        Users: <%= @group.users.count %><br />
      </div>
    </div>
  <% end %>

</div>

<div class="photo-row-wrapper">
  <div class="photo-row">
    <% @group.photos.latest.limit(10).each do |photo| %>
      <div class="picture">
        <%= image_tag photo.picture.url(:small), alt: '', class: 'pure-img' %>
      </div>
    <% end %>
  </div>
</div>

